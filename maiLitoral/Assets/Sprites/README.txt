Here we add the Unity sprites/UI elements.
Each UI composed element (for example the search bar that includes the search button sprite) must be in the same folder.
Every other composed element must be stored in a separate folder.

