Here we add the Unity scenes.
Each menu, level, page has it's own scene.
If a feature is composed from multiple scene (for example a menu that has settings, level1, level2, etc) we make a folder for the entire scene collection.

